# Run debpkg.txt or debpkg_minimal.txt first to bring
# in basic packages

# Install FEniCS manually because of the dist-upgrade
# FEniCS
$ sudo add-apt-repository ppa:fenics-packages/fenics
$ sudo apt-get update
fenics
#$ sudo apt-get dist-upgrade
